Dependencies:
	Ubuntu:
		sudo apt-get install imagemagick libmagick++-dev libtinyxml-dev
	Mac OSX:
		sudo port install tinyxml

Compile:
	1. mkdir build
	2. cd build
	3. cmake ../
	4. make
	5. ./kohonen --help
