#ifndef _FUNCS_hpp
#define _FUNCS_hpp

#include <sstream>
#include <vector>

using namespace std;

// Функция разбитвает строку по заданному символу
vector<string> split(string source, char split_char);

#endif